# 飞书应用凭证（必填）
FEISHU_APP_ID=cli_xxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenCode 服务地址（可选；仅非标准部署时需要，默认 http://localhost:4096）
OPENCODE_BASE_URL=http://localhost:4096

# OpenCode 工作目录（可选）
# OPENCODE_DIRECTORY=/path/to/workspace

# 默认模型（可选，格式 provider/model）
# OPENCODE_MODEL=openai/gpt-4o

# 默认 Agent（可选，如 build、plan、general，与 OpenCode 配置一致）
# OPENCODE_AGENT=build

# 超时毫秒（可选，默认 120000）
# OPENCODE_TIMEOUT=120000

# 思考中延迟毫秒（可选，默认 2500）
# BOT_THINKING_DELAY=2500

# 是否启用流式响应（可选，默认 true）
# BOT_ENABLE_STREAMING=true

# 流式更新间隔毫秒（可选，默认 1000）
# BOT_STREAM_INTERVAL=1000

# 群聊静默监听模式（可选，默认 true）
# 启用后群聊中所有消息转发给 OpenCode 作为上下文，仅在 bot 被直接 @提及时才回复
# BOT_GROUP_FILTER=true
