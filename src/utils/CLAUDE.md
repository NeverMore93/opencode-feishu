# CLAUDE.md

## 目录职责

- 本目录存放共享的底层工具函数。
- 这里应该优先承载纯函数、小型 helper 和跨目录可复用的无业务状态逻辑。

## 可以在这里放

- 字符串、时间、集合、类型守卫、轻量转换等工具代码。

## 不要在这里放

- 网络请求、会话状态、飞书 SDK 调用、run 编排。
- 只有单个模块会用到、且没有抽象价值的“顺手塞一下”代码。

## 修改约束

- 优先保持无副作用、易测试、依赖最少。
